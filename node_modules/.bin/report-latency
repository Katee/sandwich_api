#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../restify/bin/report-latency" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../restify/bin/report-latency" "$@"
  ret=$?
fi
exit $ret
